name: Deploy Backend to GCP

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 34.50.91.231
          username: farrelayman
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo -iu fikri_risyad10 bash -lc 'cd vkg-profiling && git pull origin main && bash scripts/deploy-backend.sh'
