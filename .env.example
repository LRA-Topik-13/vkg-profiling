# MySQL — matches setup.cli and compsci.docker.properties
MYSQL_DATABASE=compsci
MYSQL_USER=compsci
MYSQL_PASSWORD=compscipwd
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_PORT=3306

# PostgreSQL — matches setup.cli and mathsci.docker.properties
POSTGRES_DB=mathsci
POSTGRES_USER=mathsci
POSTGRES_PASSWORD=mathscipwd
POSTGRES_PORT=5432

MSSQL_SA_PASSWORD=Rootpassword1!
MSSQL_USER=academics
MSSQL_PASSWORD=academicspwd
MSSQL_PORT=1433

# Ontop
ONTOP_DEV_MODE=true

# FastAPI
API_PORT=8000

# "ontology" = derive classes/properties from university.ttl at startup (recommended)
# "hardcoded" = use the static fallback lists baked into metadata_router.py
METADATA_SOURCE=hardcoded
ONTOLOGY_FILE=/app/vkg/obda/university.ttl
OBDA_FILE=/app/vkg/obda/university.obda

# Point the API at the clean (official) or defected SPARQL endpoint.
# Clean:    http://ontop-teiid:8080/sparql
# Defected: http://ontop-teiid-defected:8080/sparql
ONTOP_ENDPOINT=http://ontop-teiid:8080/sparql

# ── Defected stack (correctness evaluation) ────────────────────────────────────
# Which defect dataset the defected stack loads: datasets_defect/level_<L>/<metric>.
# DEFECT_LEVEL ∈ {00,02,05,10,20}; DEFECT_METRIC ∈ {population,attribute,interlinking,schema}.
# Each metric is degraded in isolation so its overall completeness ≈ (100−level)%.
DEFECT_LEVEL=20
DEFECT_METRIC=population
# Host ports for the parallel defected stack (kept distinct from the clean stack).
MYSQL_DEFECTED_PORT=3308
POSTGRES_DEFECTED_PORT=5434
MSSQL_DEFECTED_PORT=1434
ONTOP_DEFECTED_PORT=8089
API_DEFECTED_PORT=8001
FRONTEND_DEFECTED_PORT=5174
